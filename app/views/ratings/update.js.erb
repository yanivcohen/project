$('#avg_star').raty({
  readOnly: true,
  score: <%= @professor.average_rating %>,
  path: '/assets'
});

$('#single_star').raty({
  readOnly: true,
  score: <%= @rating.score %>,
  path: '/assets'
});

$('#user_star').raty({
  score: <%= @rating.score %>,
  path: '/assets',
  click: function(score, evt) {
    $.ajax({
      url: '/ratings/' + <%= @rating.id %>,
      type: 'PATCH',
      data: { score: score }
    }).done(function (data){});
  }
});
